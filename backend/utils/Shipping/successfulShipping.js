const pool = require('../../pool');

exports.successfulShipping = () => {
    pool.getConnection((err) => {
        if(err){
            res.send({
                data: null,
                msg: err.message,
                status: 0
            })
        }
        else{
            let fetch = 'SELECT * FROM shipping WHERE shippingStatus = "successful";';
            pool.query(fetch, (err,result) => {
                if(err){
                    res.send({
                        data: null,
                        msg: err.message,
                        status: 0
                    })
                }
                else{
                    res.send({
                        status: 1,
                        msg: 'Successful Shipping List',
                        data: result,
                    });
                }
            })
        }
    })
}
